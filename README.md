# Frontend for Azure Serverless Microservices

This React/Vite app fetches data from 3 Azure microservices:

- Users Service (`/users`)
- Orders Service (`/orders`)
- Notification Service (`/notify`)

## Install
